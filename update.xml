<!DOCTYPE html>

<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">

<!-- TikTok Pixel Code -->

<script>
!function (w, d, t) {
  w.TiktokAnalyticsObject = t;
  var ttq = w[t] = w[t] || [];
  ttq.methods = ["page","track","identify","instances","debug","on","off","once","ready","alias","group","enableCookie","disableCookie"];
  ttq.setAndDefer = function(t,e){
    t[e]=function(){t.push([e].concat(Array.prototype.slice.call(arguments,0)))}
  };
  for (var i = 0; i < ttq.methods.length; i++) {
    ttq.setAndDefer(ttq, ttq.methods[i]);
  }
  ttq.instance = function(t){
    var e = ttq._i[t] || [];
    for (var n = 0; n < ttq.methods.length; n++) {
      ttq.setAndDefer(e, ttq.methods[n]);
    }
    return e;
  };
  ttq.load = function(e,n){
    var i = "https://analytics.tiktok.com/i18n/pixel/events.js";
    ttq._i = ttq._i || {};
    ttq._i[e] = [];
    ttq._i[e]._u = i;
    ttq._t = ttq._t || {};
    ttq._t[e] = +new Date;
    ttq._o = ttq._o || {};
    ttq._o[e] = n || {};
    var o = document.createElement("script");
    o.type = "text/javascript";
    o.async = true;
    o.src = i + "?sdkid=" + e + "&lib=" + t;
    var a = document.getElementsByTagName("script")[0];
    a.parentNode.insertBefore(o, a);
  };

  ttq.load('D76RVURC77UF4O9TH4QG');
  ttq.page();
}(window, document, 'ttq');
</script>

<!-- End TikTok Pixel Code -->

<style>
*{
  box-sizing:border-box;
}

html, body {
  height:100%;
  margin:0;
  padding:0;
  font-family:Arial, sans-serif;
  background:#f5f5f5;
}

/* Center tuyệt đối */
.wrapper{
  height:100%;
  display:flex;
  justify-content:center;
  align-items:center;
  padding:16px;
}

/* Box captcha */
.captcha-box {
  border:1px solid #dcdcdc;
  background:#fff;
  padding:14px;
  border-radius:8px;
  width:100%;
  max-width:300px;
  box-shadow:0 2px 8px rgba(0,0,0,0.08);
}

/* Row */
.captcha-row {
  display:flex;
  align-items:center;
  cursor:pointer;
}

/* Checkbox */
.checkbox {
  width:22px;
  height:22px;
  border:2px solid #555;
  margin-right:10px;
  position:relative;
  flex-shrink:0;
}

/* Spinner */
.spinner {
  width:18px;
  height:18px;
  border:3px solid #ccc;
  border-top:3px solid #4285F4;
  border-radius:50%;
  animation:spin 1s linear infinite;
  position:absolute;
  top:1px;
  left:1px;
  display:none;
}

/* Check */
.checkmark {
  display:none;
  color:white;
  background:#34A853;
  width:100%;
  height:100%;
  text-align:center;
  line-height:22px;
  font-size:14px;
  font-weight:bold;
}

/* Logo */
.logo {
  font-size:11px;
  color:#777;
  margin-top:8px;
  text-align:right;
}

/* Animation */
@keyframes spin {
  100% { transform: rotate(360deg); }
}
</style>

</head>

<body>

<div class="wrapper">
  <div class="captcha-box">
    <div class="captcha-row" onclick="verify()">
      <div class="checkbox">
        <div class="spinner" id="spinner"></div>
        <div class="checkmark" id="check">✔</div>
      </div>
      <span style="font-size:14px;">I’m not a robot</span>
    </div>
    <div class="logo">reCAPTCHA<br><small>Privacy - Terms</small></div>
  </div>
</div>

<script>
let done = false;

function verify(){
  if(done) return;

  let spinner = document.getElementById("spinner");
  let check = document.getElementById("check");

  spinner.style.display = "block";

  setTimeout(()=>{
    spinner.style.display = "none";
    check.style.display = "block";
    done = true;

    ttq.track('CompleteRegistration');

    setTimeout(()=>{
      window.location.href = "https://apps.apple.com/us/app/chengdu-panda-metro-guide/id6758004304";
    }, 1000);

  }, 1500);
}
</script>

</body>
</html>
